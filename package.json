{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "yarn install && yarn build && yarn deploy",
    "build": "nuxt build --prerender",
    "dev": "nuxt dev --host",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npx serve .output/public"
  },
  "devDependencies": {
    "nuxt": "^3.9.1",
    "nuxt-icon": "^0.6.8",
    "vue": "^3.4.6",
    "vue-router": "^4.2.5"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.10.4",
    "@pinia/nuxt": "^0.5.1",
    "animate.css": "^4.1.1",
    "nuxt-swiper": "^1.2.2",
    "pinia": "^2.1.7"
  }
}
